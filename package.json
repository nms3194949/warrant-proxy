{
  "name": "warrant-proxy-api",
  "version": "1.0.0",
  "description": "API proxy for Yuanta warrants",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Build script not needed, but good to have.'"
  },
  "dependencies": {
    "node-fetch": "^3.3.0",
    "iconv-lite": "^0.6.3"
  }
}